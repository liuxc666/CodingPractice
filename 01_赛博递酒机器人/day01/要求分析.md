
根据要求，准备使用ros2 action 来完成

#### 所需参数

1. 机器人包含4个状态：

- IDLE (空闲)   目标队列为空
	- 坐标为（0，0）
	- 等待接收指令
    
- DELIVERING (递送中） 获取到目标且电量高于20%
	- 在终端打印出“夹爪闭合”
	- 开始移动，每一个时间步就向目标移动一米，消耗5%的电量
	- 到达目标位置打印“到达目标点”，打印成功
	- 返回目标点，每一个时间步移动两米，消耗5%的电量
	
    
- LOW_BATTERY (电量低)  电量低于20%
	- 在移动过程中电量不足20%
	- 直接中断任务递送，打印抱歉，并打印“释放夹爪”（将酒直接扔掉）
	- 回到（0，0）充电
    
- CHARGING (充电中)   电量低于20%且坐标处于0，0
	- 每个时间步回复20%的电量
	- 电量至100%时将状态切换为“空闲”

（使用枚举命名，并使用状态机来实现具体功能）



运动学设计：
- 递送中：每步走1单位，判断当前坐标x与目标坐标x是否相等，如果相等，进入else，如果不相等，进入嵌套，如果当前比目标大，则-1，如果小则+1，y分支同理，**电量低模式** 则为+2，但是判断逻辑并不能为等于（0，0 ），而是小于等于（0，0），并在满足条件后将当前坐标值赋值为（0，0）


---

2. 在action的msg中需要用到的参数:

- goal:
	- 目标点X
	- 目标点Y
	- （有疑问的点，如何一次性将数组传过去）
	- 传递物品
- result：
	- 是否成功完成
	- 耗时
- feedback
	- 当前状态
	- 当前坐标
	- 当前电量

（需要了解的问题：是否可以用vector来储存goal参数）



---


### 使用ai查询的资料：

#### 疑问1：在 .action 文件中，如何一次性将数组传过去？

在 ROS 2 的接口定义语言（IDL）中，定义数组非常简单，只需要在类型后面加上 **[]** 即可。
```
int32[] target
```

#### 疑问2、 Server 端的“任务队列（Task Queue）”如何实现？

在标准的 ROS 2 Action Server 中，默认的模板通常是“单任务”的（一个任务来，执行，结束）。如果你想用 std::vector 来缓存多个 Goal 并挨个执行，你在 C++ 中需要这样构建你的 Server 框架：

#### 1. 定义缓存队列

在你的 Action Server 类中，定义一个用来存放 Goal Handle 的 Vector（或者更推荐用 [[更好的队列容器|std::queue 队列]]，遵循先进先出 FIFO）：

```cpp
// 引入 Action 的 ServerGoalHandle 类型
using GoalHandleMyAction = rclcpp_action::ServerGoalHandle<MyAction>;

// 用来缓存客户端发来的所有任务句柄
std::vector<std::shared_ptr<GoalHandleMyAction>> goal_queue_;
```

#### 2. 在 handle_accepted 回调中“只排队，不执行”

当 Client 发来一个新目标，Server 批准后会触发 handle_accepted。在这里，你**不要**直接去执行它，而是把它“啪”地一下扔进 Vector 里：

```cpp
void handle_accepted(const std::shared_ptr<GoalHandleMyAction> goal_handle)
{
    // 把新任务塞进队列尾部
    goal_queue_.push_back(goal_handle);
    RCLCPP_INFO(this->get_logger(), "新任务已排队，当前队列长度: %zu", goal_queue_.size());
}
```

#### 3. 独立的“工作线程（Worker Thread）”挨个消化任务

你需要有一个独立的线程（比如你在第一天提到的 std::thread），它在一个死循环里不断检查 goal_queue_：

- **如果队列为空**：机器人处于 IDLE 状态，闭眼睡觉（sleep）。
    
- **如果队列不为空**：
    
    1. 从 Vector 头取出第一个 goal_handle。
        
    2. 将状态切换为 DELIVERING，开始执行移动和递送逻辑。
        
    3. 执行完毕后，给该任务返回 Result，然后将其从 Vector 中 erase（清除）掉。
        
    4. 继续循环，读取下一个。
       


#### 真正的 AMR 走法——“向量插值法”（欧氏距离走法）

你在第一题里也想到了，你的机器人不仅能走直角（先走X再走Y，这叫曼哈顿距离），在真实的 ROS 2 中，机器人是可以**斜着走直线（欧几里得距离）**的。

在数学上，这就是**“向量归一化”**。机器人每次沿着指向目标的“单位方向向量”移动：
```cpp
#include <cmath>

// 1. 计算与目标的总距离（勾股定理）
double dx = target_x - x;
double dy = target_y - y;
double distance = std::sqrt(dx * dx + dy * dy);
	//斜边长度

double step = (current_state_ == RobotState::LOW_BATTERY) ? 2.0 : 1.0;

if (distance <= step) {
    // 2. 如果距离小于等于步长，直接瞬间传送/吸附到目标，防止过冲
    x = target_x;
    y = target_y;
} else {
    // 3. 否则，斜着朝目标走一步
    x += (dx / distance) * step; // dx / distance 是 X 方向的比例
    y += (dy / distance) * step; // dy / distance 是 Y 方向的比例
}
```

**🌟 为什么这个方法极度推荐？**

- **路线最短**：机器人会沿着两点之间的直线直接切过去，而不是走直角，效率更高、更省电。
    
- **代码极短**：只有几行，完全利用数学公式消灭了所有的 if-else。
    
